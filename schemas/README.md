# Schemas

Machine-readable report schemas (JSON Schema) so reports can be generated,
validated, and exchanged programmatically.

Suggested starter schemas:

- `bug-report.schema.json`
- `regression-summary.schema.json`
- `release-sign-off.schema.json`
- `test-plan-status.schema.json`
